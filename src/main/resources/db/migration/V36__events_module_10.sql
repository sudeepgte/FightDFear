ALTER TABLE event_hosts
    ADD COLUMN IF NOT EXISTS whatsapp_number VARCHAR(32),
    ADD COLUMN IF NOT EXISTS pincode VARCHAR(16),
    ADD COLUMN IF NOT EXISTS latitude DOUBLE,
    ADD COLUMN IF NOT EXISTS longitude DOUBLE,
    ADD COLUMN IF NOT EXISTS audience TEXT,
    ADD COLUMN IF NOT EXISTS door_service BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS facilities TEXT,
    ADD COLUMN IF NOT EXISTS open_days VARCHAR(255),
    ADD COLUMN IF NOT EXISTS open_time TIME,
    ADD COLUMN IF NOT EXISTS close_time TIME,
    ADD COLUMN IF NOT EXISTS break_start TIME,
    ADD COLUMN IF NOT EXISTS break_end TIME,
    ADD COLUMN IF NOT EXISTS blocked_dates TEXT,
    ADD COLUMN IF NOT EXISTS credential_number VARCHAR(128),
    ADD COLUMN IF NOT EXISTS session_mode VARCHAR(64),
    ADD COLUMN IF NOT EXISTS duration_minutes INT,
    ADD COLUMN IF NOT EXISTS buffer_minutes INT,
    ADD COLUMN IF NOT EXISTS typical_price DOUBLE,
    ADD COLUMN IF NOT EXISTS upi_id VARCHAR(128),
    ADD COLUMN IF NOT EXISTS bank_details TEXT,
    ADD COLUMN IF NOT EXISTS payout_balance DOUBLE DEFAULT 0,
    ADD COLUMN IF NOT EXISTS payout_requested_at DATETIME,
    ADD COLUMN IF NOT EXISTS gallery_photos TEXT,
    ADD COLUMN IF NOT EXISTS rating DOUBLE DEFAULT 0,
    ADD COLUMN IF NOT EXISTS review_count INT DEFAULT 0;

ALTER TABLE women_event_registrations
    ADD COLUMN IF NOT EXISTS payout_credited BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS coach_notes TEXT;
